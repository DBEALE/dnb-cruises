{
  "name": "dnb-cruises",
  "version": "1.0.0",
  "description": "Multi-provider cruise scraper and viewer for the UK market",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "node scripts/generate-build-info.js",
    "test": "node --test test/ui-provider-load.test.js && playwright test",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "cheerio": "^1.2.0",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
